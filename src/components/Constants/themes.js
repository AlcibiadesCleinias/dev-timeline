import { createTheme } from "@mui/material";

export const THEME = createTheme({
  palette: {
    warning: {
      light: "rgba(255,215,0,0.49)",
      main: "#FFD700",
      dark: "#ffc400",
      contrastText: "#fff",
    },
  },
});
